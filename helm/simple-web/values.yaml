nameOverride: simple-web
namespace: secure-path-dev

image:
  repository: localhost:9092/secure-delivery/simple-web
  tag: dev
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

containerPort: 80

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 250m
    memory: 128Mi

